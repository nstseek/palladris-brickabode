import React from 'react';
import PropTypes from 'prop-types';
import './BlotterChart.scss';

const BlotterChart = () => (
  <div className="BlotterChart">
    BlotterChart Component
  </div>
);

BlotterChart.propTypes = {};

BlotterChart.defaultProps = {};

export default BlotterChart;
